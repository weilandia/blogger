<h1>Articles Tagged with <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li><%= link_to article.title, article_path(article.id) %></li>
  <% end %>
</ul><br>

<%= link_to "Delete", tag_path(@tag.id), method: :delete, data: {confirm: "Are you sure?"} %>
